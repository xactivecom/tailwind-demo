Install Tailwind CSS developer:
- npm i -D tailwindcss

Build CSS once:
- npm run build

Build CSS continuously:
- npm run watch

Build CSS continuously:
- npx @tailwindcss/cli -i ./src/input.css -o ./src/css/style.css --watch
